
## 2026-03-05 11:31 HKT – Bus‑alarm Git push & CTB station‑name alignment
- Added `getStopDetails(stopId)` helper to fetch Citybus stop name (TC/EN/SC) via `/stop/{stop_id}`.
- Updated `refreshRouteInformation` for CTB/NWFB to enrich each route stop with `name_tc`, `name_en`, `name_sc`.
- Adjusted `OPERATORS` baseUrl for CTB/NWFB to `https://rt.data.gov.hk/v2/transport/citybus`.
- Tested API: `GET https://rt.data.gov.hk/v2/transport/citybus/route-stop/CTB/23/outbound` returns 28 stops with names after enrichment.
- Restarted server with `.env` set to `BUS_OPERATOR=ctb`, `BUS_ROUTE=23`; server now reports "✓ Updated route information for Citybus (城巴) 23. Found 28 stops (with names)."
- Attempted `git push` to `git@github.com:louislsh68/Bus-alarm.git` → host key verification failed (no SSH key). Provided two push options:
  1. Use HTTPS with personal access token (PAT) – set remote to `https://<TOKEN>@github.com/louislsh68/Bus-alarm.git` then `git push -u origin main`.
  2. Add generated SSH public key to GitHub → then push via original SSH URL.
- Mentioned that after providing PAT or adding SSH key, assistant can complete the push.
