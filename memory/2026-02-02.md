2026-02-02

- Backend prototype successfully enhanced to fetch real-time arrival data (ETA) from HK government transport APIs.
- Dynamic fetching of route-stop mappings (for CTB) failed with a 422 error on the endpoint `https://rt.data.gov.hk/v2/transport/citybus/route-stop/ctb`. This prevented automatic population of the route/stop lookup table.
- Implemented a `STATIC_ROUTE_STOP_MAPPING` as a fallback mechanism. The system now uses this static data to resolve stop names to IDs when the dynamic fetch fails.
- The schedule checker now correctly determines the company ID (CTB/KMB) by first checking if the route/direction/stop exists in the static map, improving accuracy over the previous heuristic.
- Successfully simulated the iOS app workflow: saving user settings (schedule, route, stop, direction) via the POST API and confirmed the backend can process these settings using the improved logic.
- The backend can now proceed to call the ETA API (e.g., CTB's) with the correct parameters derived from the static map and user settings.
- KMB API integration remains a placeholder needing further investigation into its specific endpoint requirements.